<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="fun.krisme.smartbus"
    >

    <application

        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ConsultActivity"
            android:label="咨询"></activity>
        <activity android:name=".TrainingActivity"
            android:label="培训"></activity>
        <activity android:name=".SocialSecurityActivity"
            android:label="社会保障"></activity>
        <activity android:name=".TaxDeputyActivity"
            android:label="税务代理"></activity>
        <activity android:name=".FinancialSharingActivity"
            android:label="财务共享">

        </activity>
        <activity android:name=".ChiefActivity"
            android:label="大智移云"></activity>
        <activity android:name=".RegistActivity" />
        <activity android:name=".MainActivity"
            android:label="大智移云">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>